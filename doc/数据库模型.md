# 数据库模型 - 完整使用指南

> 🗄️ **文档位置**: `doc/数据库模型.md`
> 
> **关联文件**:
> - `backend/pkg/db/mysql.go` - 数据库连接和初始化
> - `backend/app/models/*.go` - 数据模型定义
> - `backend/internal/config/config.go` - 数据库配置

---

## 📋 目录

1. [架构概览](#架构概览)
2. [配置说明](#配置说明)
3. [数据库连接](#数据库连接)
4. [模型定义](#模型定义)
5. [CRUD 操作](#crud-操作)
6. [事务处理](#事务处理)
7. [表结构](#表结构)
8. [最佳实践](#最佳实践)

---

## 架构概览

```
┌─────────────────────────────────────────────────────────────────┐
│                        数据访问层架构                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   Controller  ──►  Model  ──►  sqlx  ──►  MySQL Driver          │
│       │              │          │            │                  │
│       │              │          │            ▼                  │
│       │              │          │      ┌──────────┐            │
│       │              │          │      │  MySQL   │            │
│       │              │          │      │  Server  │            │
│       │              │          │      └──────────┘            │
│       │              │          │                              │
│       │              ▼          │                              │
│       │         ┌──────────┐    │                              │
│       │         │  User    │    │                              │
│       │         │  Email   │    │                              │
│       │         │  Code    │    │                              │
│       │         └──────────┘    │                              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 核心组件

| 组件 | 文件 | 说明 |
|------|------|------|
| DB 连接 | `pkg/db/mysql.go` | 数据库连接池 |
| User 模型 | `app/models/user.go` | 用户数据操作 |
| Email 模型 | `app/models/email.go` | 邮件相关操作 |
| Code 模型 | `app/models/verification_code.go` | 验证码操作 |
| Base 模型 | `app/models/base.go` | 基础模型结构 |

---

## 配置说明

### 环境变量

在 `.env` 文件中配置数据库连接：

```env
# MySQL 配置
DB_HOST=127.0.0.1      # 数据库主机
DB_PORT=3306           # 数据库端口
DB_USER=root           # 用户名
DB_PASSWORD=password   # 密码
DB_NAME=fst_platform   # 数据库名
```

### 配置代码

**文件**: `backend/internal/config/config.go`

```go
// buildDSN 构建数据源名称
func buildDSN() string {
    user := getEnv("DB_USER", "root")
    pass := getEnv("DB_PASSWORD", "")
    host := getEnv("DB_HOST", "127.0.0.1")
    port := getEnv("DB_PORT", "3306")
    name := getEnv("DB_NAME", "fst_platform")
    
    return user + ":" + pass + "@tcp(" + host + ":" + port + ")/" + name + 
        "?charset=utf8mb4&parseTime=True&loc=Local"
}
```

### DSN 参数说明

| 参数 | 值 | 说明 |
|------|-----|------|
| charset | utf8mb4 | 支持完整的 UTF-8（包括 emoji） |
| parseTime | True | 自动解析时间类型为 time.Time |
| loc | Local | 使用本地时区 |

---

## 数据库连接

### 初始化连接

**文件**: `backend/pkg/db/mysql.go`

```go
package db

import (
    "log"
    
    "github.com/jmoiron/sqlx"
    _ "github.com/go-sql-driver/mysql"
)

var DB *sqlx.DB

// InitDB 初始化数据库连接
func InitDB(dsn string) error {
    var err error
    DB, err = sqlx.Connect("mysql", dsn)
    if err != nil {
        return err
    }
    
    // 连接池配置
    DB.SetMaxOpenConns(25)    // 最大打开连接数
    DB.SetMaxIdleConns(10)    // 最大空闲连接数
    
    return nil
}
```

### 使用连接

```go
import "fst/backend/pkg/db"

// 执行查询
var users []User
err := db.DB.Select(&users, "SELECT * FROM users")

// 执行插入
result, err := db.DB.Exec("INSERT INTO users (name) VALUES (?)", name)

// 获取单行
var user User
err := db.DB.Get(&user, "SELECT * FROM users WHERE id = ?", id)
```

### 连接池最佳配置

```go
func InitDB(dsn string) error {
    DB, err = sqlx.Connect("mysql", dsn)
    if err != nil {
        return err
    }
    
    // 连接池配置
    DB.SetMaxOpenConns(25)        // 最大打开连接
    DB.SetMaxIdleConns(10)        // 最大空闲连接
    DB.SetConnMaxLifetime(5 * time.Minute)  // 连接最大生命周期
    DB.SetConnMaxIdleTime(1 * time.Minute)  // 空闲连接最大生命周期
    
    // 验证连接
    if err := DB.Ping(); err != nil {
        return err
    }
    
    return nil
}
```

---

## 模型定义

### 基础模型结构

**文件**: `backend/app/models/base.go`

```go
package models

import "time"

// BaseModel 基础模型
type BaseModel struct {
    ID        uint64    `db:"id" json:"id"`
    CreatedAt time.Time `db:"created_at" json:"created_at"`
    UpdatedAt time.Time `db:"updated_at" json:"updated_at"`
}
```

### User 模型

**文件**: `backend/app/models/user.go`

```go
package models

import (
    "database/sql"
    "time"
    "fst/backend/pkg/db"
)

// User 用户模型
type User struct {
    ID            uint64         `db:"id" json:"id"`
    GroupID       uint64         `db:"group_id" json:"group_id"`
    Username      string         `db:"username" json:"username"`
    Nickname      sql.NullString `db:"nickname" json:"nickname"`
    Email         string         `db:"email" json:"email"`
    Mobile        sql.NullString `db:"mobile" json:"mobile"`
    Avatar        sql.NullString `db:"avatar" json:"avatar"`
    BackGround    sql.NullString `db:"back_ground" json:"back_ground"`
    Gender        int            `db:"gender" json:"gender"`
    Birthday      sql.NullTime   `db:"birthday" json:"birthday"`
    Password      string         `db:"password" json:"-"`  // 不序列化到JSON
    Role          string         `db:"role" json:"role"`
    Status        int            `db:"status" json:"status"`
    LastLoginTime sql.NullTime   `db:"last_login_time" json:"last_login_time"`
    LastLoginIP   sql.NullString `db:"last_login_ip" json:"last_login_ip"`
    DeleteTime    sql.NullTime   `db:"delete_time" json:"delete_time,omitempty"`
    CreatedAt     time.Time      `db:"created_at" json:"created_at"`
    UpdatedAt     time.Time      `db:"updated_at" json:"updated_at"`
}
```

### sql.NullXXX 类型说明

| 类型 | 用途 | 示例 |
|------|------|------|
| sql.NullString | 可为 NULL 的字符串 | `sql.NullString{String: "value", Valid: true}` |
| sql.NullInt64 | 可为 NULL 的整数 | `sql.NullInt64{Int64: 123, Valid: true}` |
| sql.NullTime | 可为 NULL 的时间 | `sql.NullTime{Time: now, Valid: true}` |
| sql.NullFloat64 | 可为 NULL 的浮点数 | - |
| sql.NullBool | 可为 NULL 的布尔值 | - |

**使用示例**:
```go
// 创建用户时使用 sql.NullString
user := &User{
    Username: "john",
    Nickname: sql.NullString{String: "John Doe", Valid: true},
    Email:    "john@example.com",
    Mobile:   sql.NullString{},  // NULL
}

// 读取时检查有效性
if user.Nickname.Valid {
    fmt.Println(user.Nickname.String)
} else {
    fmt.Println("No nickname")
}
```

---

## CRUD 操作

### Create（创建）

```go
// CreateUser 创建用户
func CreateUser(user *User) error {
    query := `
        INSERT INTO users (
            group_id, username, nickname, email, mobile, 
            avatar, password, role, status
        ) VALUES (
            :group_id, :username, :nickname, :email, :mobile,
            :avatar, :password, :role, :status
        )
    `
    result, err := db.DB.NamedExec(query, user)
    if err != nil {
        return err
    }
    
    // 获取插入的ID
    id, _ := result.LastInsertId()
    user.ID = uint64(id)
    
    return nil
}

// 使用示例
user := &models.User{
    Username: "newuser",
    Email:    "new@example.com",
    Password: hashedPassword,
    Role:     "user",
    Status:   1,
}

if err := models.CreateUser(user); err != nil {
    // 处理错误
}
fmt.Printf("Created user with ID: %d\n", user.ID)
```

### Read（查询）

```go
// GetUserByID 通过ID获取用户
func GetUserByID(id uint64) (*User, error) {
    var user User
    err := db.DB.Get(&user, 
        "SELECT * FROM users WHERE id = ? AND delete_time IS NULL", 
        id,
    )
    if err == sql.ErrNoRows {
        return nil, nil
    }
    if err != nil {
        return nil, err
    }
    return &user, nil
}

// GetUserByUsername 通过用户名获取用户
func GetUserByUsername(username string) (*User, error) {
    var user User
    err := db.DB.Get(&user, 
        "SELECT * FROM users WHERE username = ? AND delete_time IS NULL", 
        username,
    )
    if err == sql.ErrNoRows {
        return nil, nil
    }
    if err != nil {
        return nil, err
    }
    return &user, nil
}

// GetUserByEmail 通过邮箱获取用户
func GetUserByEmail(email string) (*User, error) {
    var user User
    err := db.DB.Get(&user, 
        "SELECT * FROM users WHERE email = ? AND delete_time IS NULL", 
        email,
    )
    if err == sql.ErrNoRows {
        return nil, nil
    }
    if err != nil {
        return nil, err
    }
    return &user, nil
}

// GetUserByUsernameOrEmail 通过用户名或邮箱获取用户
func GetUserByUsernameOrEmail(identifier string) (*User, error) {
    var user User
    err := db.DB.Get(&user, 
        "SELECT * FROM users WHERE (username = ? OR email = ?) AND delete_time IS NULL", 
        identifier, identifier,
    )
    if err == sql.ErrNoRows {
        return nil, nil
    }
    if err != nil {
        return nil, err
    }
    return &user, nil
}

// GetUsers 获取用户列表
func GetUsers(offset, limit int) ([]User, error) {
    var users []User
    err := db.DB.Select(&users, 
        "SELECT * FROM users WHERE delete_time IS NULL ORDER BY id DESC LIMIT ? OFFSET ?", 
        limit, offset,
    )
    return users, err
}

// SearchUsers 搜索用户
func SearchUsers(keyword string) ([]User, error) {
    var users []User
    pattern := "%" + keyword + "%"
    err := db.DB.Select(&users, 
        "SELECT * FROM users WHERE delete_time IS NULL AND (username LIKE ? OR email LIKE ?)", 
        pattern, pattern,
    )
    return users, err
}
```

### Update（更新）

```go
// UpdateUser 更新用户信息
func UpdateUser(user *User) error {
    query := `
        UPDATE users SET
            nickname = :nickname,
            email = :email,
            mobile = :mobile,
            avatar = :avatar,
            role = :role,
            status = :status,
            updated_at = NOW()
        WHERE id = :id AND delete_time IS NULL
    `
    _, err := db.DB.NamedExec(query, user)
    return err
}

// UpdatePassword 更新密码
func UpdatePassword(userID uint64, hashedPassword string) error {
    _, err := db.DB.Exec(
        "UPDATE users SET password = ?, updated_at = NOW() WHERE id = ?",
        hashedPassword, userID,
    )
    return err
}

// UpdateLastLogin 更新最后登录信息
func UpdateLastLogin(userID uint64, ip string) error {
    _, err := db.DB.Exec(
        "UPDATE users SET last_login_time = NOW(), last_login_ip = ? WHERE id = ?",
        ip, userID,
    )
    return err
}

// 使用示例
user, _ := models.GetUserByID(123)
user.Nickname = sql.NullString{String: "New Name", Valid: true}
if err := models.UpdateUser(user); err != nil {
    log.Printf("Update failed: %v", err)
}
```

### Delete（删除）

```go
// SoftDeleteUser 软删除用户
func SoftDeleteUser(userID uint64) error {
    _, err := db.DB.Exec(
        "UPDATE users SET delete_time = NOW() WHERE id = ? AND delete_time IS NULL",
        userID,
    )
    return err
}

// HardDeleteUser 硬删除用户
func HardDeleteUser(userID uint64) error {
    _, err := db.DB.Exec(
        "DELETE FROM users WHERE id = ?",
        userID,
    )
    return err
}

// RestoreUser 恢复软删除的用户
func RestoreUser(userID uint64) error {
    _, err := db.DB.Exec(
        "UPDATE users SET delete_time = NULL WHERE id = ?",
        userID,
    )
    return err
}
```

---

## 事务处理

### 基本事务

```go
func TransferMoney(fromID, toID uint64, amount float64) error {
    tx, err := db.DB.Beginx()
    if err != nil {
        return err
    }
    
    // 确保事务回滚或提交
    defer func() {
        if err != nil {
            tx.Rollback()
        } else {
            err = tx.Commit()
        }
    }()
    
    // 扣款
    _, err = tx.Exec("UPDATE accounts SET balance = balance - ? WHERE id = ?", amount, fromID)
    if err != nil {
        return err
    }
    
    // 加款
    _, err = tx.Exec("UPDATE accounts SET balance = balance + ? WHERE id = ?", amount, toID)
    if err != nil {
        return err
    }
    
    // 记录交易
    _, err = tx.Exec(
        "INSERT INTO transactions (from_id, to_id, amount) VALUES (?, ?, ?)",
        fromID, toID, amount,
    )
    if err != nil {
        return err
    }
    
    return nil
}
```

### 事务封装函数

```go
// WithTransaction 事务封装
func WithTransaction(fn func(*sqlx.Tx) error) error {
    tx, err := db.DB.Beginx()
    if err != nil {
        return err
    }
    
    defer func() {
        if p := recover(); p != nil {
            tx.Rollback()
            panic(p)
        } else if err != nil {
            tx.Rollback()
        } else {
            err = tx.Commit()
        }
    }()
    
    err = fn(tx)
    return err
}

// 使用示例
err := WithTransaction(func(tx *sqlx.Tx) error {
    // 在事务中执行多个操作
    if _, err := tx.Exec("INSERT ..."); err != nil {
        return err
    }
    if _, err := tx.Exec("UPDATE ..."); err != nil {
        return err
    }
    return nil
})
```

---

## 表结构

### users 表

```sql
CREATE TABLE IF NOT EXISTS users (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY COMMENT '用户ID',
    group_id BIGINT UNSIGNED NOT NULL DEFAULT 0 COMMENT '分组ID',
    username VARCHAR(50) NOT NULL COMMENT '用户名',
    nickname VARCHAR(50) COMMENT '昵称',
    email VARCHAR(150) NOT NULL COMMENT '邮箱',
    mobile VARCHAR(20) COMMENT '手机号',
    avatar VARCHAR(255) COMMENT '头像URL',
    back_ground VARCHAR(255) COMMENT '背景图URL',
    gender TINYINT NOT NULL DEFAULT 0 COMMENT '性别 0未知 1男 2女',
    birthday DATE COMMENT '生日',
    password VARCHAR(255) NOT NULL COMMENT '密码哈希',
    role VARCHAR(20) NOT NULL DEFAULT 'user' COMMENT '角色',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态 0禁用 1启用',
    last_login_time TIMESTAMP NULL COMMENT '最后登录时间',
    last_login_ip VARCHAR(50) COMMENT '最后登录IP',
    delete_time TIMESTAMP NULL COMMENT '软删除时间',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    UNIQUE KEY idx_users_username (username),
    UNIQUE KEY idx_users_email (email),
    KEY idx_users_group (group_id),
    KEY idx_users_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

### email_logs 表

```sql
CREATE TABLE IF NOT EXISTS email_logs (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    to_email VARCHAR(150) NOT NULL COMMENT '收件人',
    subject VARCHAR(255) NOT NULL COMMENT '主题',
    content TEXT COMMENT '内容',
    template_name VARCHAR(50) COMMENT '使用的模板',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态 0失败 1成功',
    error_msg TEXT COMMENT '错误信息',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    KEY idx_email_logs_to (to_email),
    KEY idx_email_logs_created (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='邮件发送日志';
```

### email_templates 表

```sql
CREATE TABLE IF NOT EXISTS email_templates (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL COMMENT '模板标识',
    lang VARCHAR(10) NOT NULL DEFAULT 'zh-CN' COMMENT '语言',
    title VARCHAR(100) COMMENT '标题',
    subject VARCHAR(255) NOT NULL COMMENT '邮件主题',
    content TEXT NOT NULL COMMENT '邮件内容',
    description TEXT COMMENT '描述',
    variables TEXT COMMENT '可用变量说明',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态 0禁用 1启用',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    UNIQUE KEY idx_email_tpl_name_lang (name, lang),
    KEY idx_email_tpl_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='邮件模板';
```

### verification_codes 表

业务字段为 `contact`（邮箱或手机号）。SQLite 从旧版迁移时采用「加列拷贝」：新增 `contact` 并从 `email` 拷贝数据，**旧 `email` 列会残留**——属设计如此，不是漏迁移。MySQL 侧可 `CHANGE COLUMN email contact`。

```sql
CREATE TABLE IF NOT EXISTS verification_codes (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    email VARCHAR(255) NOT NULL COMMENT '邮箱地址',
    code VARCHAR(10) NOT NULL COMMENT '验证码',
    code_type VARCHAR(50) NOT NULL COMMENT '类型 register/reset_password',
    expires_at TIMESTAMP NOT NULL COMMENT '过期时间',
    is_used TINYINT NOT NULL DEFAULT 0 COMMENT '是否已使用',
    is_deleted TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除（软删）',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_email_type (email, code_type),
    INDEX idx_expires (expires_at),
    INDEX idx_created (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='验证码表';
```

---

## 最佳实践

### 1. 使用 Named Query

```go
// ✅ 推荐：使用命名参数
query := `
    INSERT INTO users (username, email, password)
    VALUES (:username, :email, :password)
`
result, err := db.DB.NamedExec(query, user)

// ❌ 不推荐：位置参数容易出错
query := "INSERT INTO users (username, email) VALUES (?, ?)"
result, err := db.DB.Exec(query, user.Username, user.Email)
```

### 2. 软删除优先

```go
// ✅ 推荐：软删除
func DeleteUser(id uint64) error {
    _, err := db.DB.Exec(
        "UPDATE users SET delete_time = NOW() WHERE id = ?",
        id,
    )
    return err
}

// 查询时排除已删除
func GetUser(id uint64) (*User, error) {
    var user User
    err := db.DB.Get(&user, 
        "SELECT * FROM users WHERE id = ? AND delete_time IS NULL",
        id,
    )
    return &user, err
}
```

### 3. 错误处理

```go
func GetUserByID(id uint64) (*User, error) {
    var user User
    err := db.DB.Get(&user, "SELECT * FROM users WHERE id = ?", id)
    
    if err == sql.ErrNoRows {
        // 记录但不视为错误
        log.Printf("User %d not found", id)
        return nil, nil
    }
    if err != nil {
        // 包装错误信息
        return nil, fmt.Errorf("failed to get user %d: %w", id, err)
    }
    
    return &user, nil
}
```

### 4. 批量操作

```go
// 批量插入
func CreateUsers(users []User) error {
    query := "INSERT INTO users (username, email) VALUES (:username, :email)"
    _, err := db.DB.NamedExec(query, users)
    return err
}

// 批量更新（使用 CASE WHEN）
func UpdateUserStatuses(updates map[uint64]int) error {
    if len(updates) == 0 {
        return nil
    }
    
    // 构建动态 SQL
    var ids []uint64
    var cases []string
    for id, status := range updates {
        ids = append(ids, id)
        cases = append(cases, fmt.Sprintf("WHEN %d THEN %d", id, status))
    }
    
    query := fmt.Sprintf(`
        UPDATE users 
        SET status = CASE id %s END,
            updated_at = NOW()
        WHERE id IN (?)
    `, strings.Join(cases, " "))
    
    query, args, _ := sqlx.In(query, ids)
    query = db.DB.Rebind(query)
    _, err := db.DB.Exec(query, args...)
    return err
}
```

### 5. 分页查询

```go
type Pagination struct {
    Page     int `json:"page" form:"page"`
    PageSize int `json:"pageSize" form:"pageSize"`
}

func (p *Pagination) Offset() int {
    return (p.Page - 1) * p.PageSize
}

func (p *Pagination) Limit() int {
    if p.PageSize <= 0 {
        return 10
    }
    if p.PageSize > 100 {
        return 100
    }
    return p.PageSize
}

// 分页查询
func GetUsersPaged(p Pagination) ([]User, int64, error) {
    var users []User
    var total int64
    
    // 获取总数
    err := db.DB.Get(&total, "SELECT COUNT(*) FROM users WHERE delete_time IS NULL")
    if err != nil {
        return nil, 0, err
    }
    
    // 获取数据
    err = db.DB.Select(&users, 
        "SELECT * FROM users WHERE delete_time IS NULL ORDER BY id DESC LIMIT ? OFFSET ?",
        p.Limit(), p.Offset(),
    )
    
    return users, total, err
}
```

### 6. 避免 SQL 注入

```go
// ✅ 安全：使用参数化查询
username := c.Query("username")
user, err := models.GetUserByUsername(username)  // 内部使用 ? 占位符

// ❌ 危险：字符串拼接
query := fmt.Sprintf("SELECT * FROM users WHERE username = '%s'", username)  // 永远不要这样！
```

---

## API 参考

### models/user.go

| 函数 | 签名 | 说明 |
|------|------|------|
| CreateUser | `func CreateUser(user *User) error` | 创建用户 |
| GetUserByID | `func GetUserByID(id uint64) (*User, error)` | 通过ID获取用户 |
| GetUserByUsername | `func GetUserByUsername(username string) (*User, error)` | 通过用户名获取 |
| GetUserByEmail | `func GetUserByEmail(email string) (*User, error)` | 通过邮箱获取 |
| GetUserByUsernameOrEmail | `func GetUserByUsernameOrEmail(identifier string) (*User, error)` | 用户名或邮箱 |
| UpdateUser | `func UpdateUser(user *User) error` | 更新用户 |
| UpdatePassword | `func UpdatePassword(userID uint64, hashedPassword string) error` | 更新密码 |
| SoftDeleteUser | `func SoftDeleteUser(userID uint64) error` | 软删除用户 |

### models/email.go

| 函数 | 签名 | 说明 |
|------|------|------|
| CreateEmailLog | `func CreateEmailLog(to, subject, content, tplName string, status int, errorMsg string) error` | 创建邮件日志 |
| GetEmailTemplate | `func GetEmailTemplate(name, lang string) (*EmailTemplate, error)` | 获取邮件模板 |

### models/verification_code.go

| 函数 | 签名 | 说明 |
|------|------|------|
| CreateVerificationCode | `func CreateVerificationCode(email, code, codeType string, expiresAt time.Time) error` | 创建验证码 |
| VerifyCode | `func VerifyCode(email, code, codeType string) (bool, uint64, error)` | 验证验证码 |
| MarkVerificationCodeAsUsed | `func MarkVerificationCodeAsUsed(codeID uint64) error` | 标记已使用 |

---

> 📝 **最后更新**: 2026-02-04
> 
> 如有疑问，请参考 `backend/app/models/*.go` 源代码。
