# 用户认证与请求处理控制器 (Controllers)

> 路径：`backend/app/controllers/`  
> **最后更新**：2026-08-09

## 简介

控制器接收 HTTP、校验参数、调用 services、返回统一响应。按 **public / user / admin / system** 分一级目录，每级目录下再按业务分二级目录。包名英文，`@Tags` 中文；相同业务在不同 scope 下可同名，由 routes 用 import alias 区分。

## 目录（当前）

```text
controllers/
├── public/
│   ├── auth/        # 登录注册
│   ├── session/     # 会话强退
│   ├── geo/         # 区号/地理
│   ├── settings/    # 公开配置
│   └── payment/     # 支付回调
├── user/
│   ├── profile/     # 资料、余额/积分/操作/API日志
│   ├── payment/     # 用户支付订单
│   ├── realname/    # 实名认证
│   ├── announcement/# 公告
│   └── withdraw/    # 提现申请
├── admin/
│   ├── announcement/  # 公告管理
│   ├── apilog/        # API 访问日志
│   ├── autojob/       # 自动任务
│   ├── dashboard/     # 仪表盘
│   ├── dbconsole/     # 数据库控制台
│   ├── debug/         # debug/pprof/终端/重启
│   ├── emaillog/      # 邮件日志
│   ├── emailtemplate/ # 邮件模板
│   ├── log/           # 操作日志
│   ├── online/        # 在线用户
│   ├── payment/       # 支付订单/对账/网关
│   ├── profile/       # 管理员改密
│   ├── realname/      # 实名审核
│   ├── settings/      # 系统设置
│   ├── smslog/        # 短信日志
│   ├── smstemplate/   # 短信模板
│   ├── terminal/      # 调试终端
│   ├── todo/          # 待办（演示）
│   ├── user/          # 用户 CRUD
│   ├── userlevel/     # 用户等级
│   ├── usermoney/     # 余额/积分管理
│   └── withdraw/      # 提现审核/打款
├── system/
│   └── system/        # 系统状态/清理状态
└── v2/
    └── test/          # V2 测试接口（占位）
```

## 分层与权限

| 层 | 鉴权 | 示例 |
|----|------|------|
| public | 无 | login、app-config、payment notify/return |
| user | user 或 admin token | profile、充值下单、提现申请 |
| admin | admin + AdminOnly | 用户 CRUD、补单、设置 |

管理端 URL 前缀由路由层 `ADMIN_API_PATH` 决定，控制器内相对路径注册到 `adminGroup`。

## 高危接口（2026-07）

- **LoginToUser（代登录）**：生产禁止；禁止代登管理员；目标须启用；短 TTL；安全审计日志。
- **Debug / pprof / 重启 / 手动任务**：`IsAdminDebugOpsEnabled()`，生产永远关。
- **支付回调**：参数日志脱敏；入账前服务层绑定校验。

## Swagger 注解

仍可写 `@Router /api/v1/admin/...`。自定义 `ADMIN_API_PATH` 时由中间件改写 doc.json。

## 规范

- 业务逻辑下沉 services，控制器保持薄。
- 失败统一 `utils.Fail`，成功 `utils.Success`。
- 返回 message 统一英文（自 2026-08-09 起）。
- 推荐每个 controller 实现 `RegisterRoutes(group *gin.RouterGroup)`，由 `routes.go` 按 public / user / admin / system 挂载；少数 CRUD 较薄的 controller 也可由 `routes.go` 直接按方法挂载。
- 包名：二级目录英文（如 `auth`、`payment`、`usermoney`），一个文件一个 controller；测试文件集中在 `backend/routes/`。
