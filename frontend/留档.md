# Frontend 目录留档

> 📁 **文件位置**: `frontend/留档.md`
> 
> **最后更新**: 2026-02-04

---

## 📂 目录结构

```
frontend/
├── src/                    # 源代码
│   ├── components/         # 公共组件
│   │   ├── common/                 # 通用组件
│   │   └── custom/                 # 业务组件
│   ├── views/              # 页面视图
│   │   ├── build-in/               # 内置页面
│   │   │   └── login/              # 登录相关
│   │   ├── dashboard/              # 仪表盘
│   │   ├── setting/                # 设置
│   │   └── demo/                   # 演示页面
│   ├── router/             # 路由配置
│   │   └── index.ts                # 路由定义
│   ├── store/              # 状态管理 (Pinia)
│   │   ├── auth.ts                 # 认证状态
│   │   └── app.ts                  # 应用状态
│   ├── service/            # API 请求
│   │   ├── request.ts              # 请求封装 (Alova)
│   │   └── api/                    # API 定义
│   │       ├── login.ts            # 登录API
│   │       ├── user.ts             # 用户API
│   │       └── system.ts           # 系统API
│   ├── typings/            # TypeScript 类型
│   │   ├── entities/               # 实体类型
│   │   ├── app-route.d.ts          # 路由类型
│   │   └── request.d.ts            # 请求类型
│   ├── locales/            # 国际化
│   │   ├── zh_CN.json              # 中文
│   │   └── en_US.json              # 英文
│   ├── constants/          # 常量定义
│   │   └── Regex.ts                # 正则表达式
│   └── utils/              # 工具函数
│       └── index.ts                # 通用工具
├── public/                 # 静态资源
├── index.html              # HTML 模板
├── vite.config.ts          # Vite 配置
├── tsconfig.json           # TypeScript 配置
└── package.json            # 依赖配置
```

---

## 🚀 快速开始

### 安装依赖

```bash
cd frontend
pnpm install
```

### 启动开发服务器

```bash
pnpm dev
```

### 构建

```bash
pnpm build
```

---

## 📚 核心模块

### 1. 请求系统

**关键文件**:
- `src/service/request.ts` - Alova 请求封装
- `src/service/api/*.ts` - API 定义

**使用方式**:
```typescript
import { fetchLogin, fetchGetUserProfile } from '@/service'

// 登录
const res = await fetchLogin({ username, password })

// 获取用户信息
const user = await fetchGetUserProfile()
```

📖 **详细文档**: [../doc/前端请求.md](../doc/前端请求.md)

### 2. 路由系统

**关键文件**:
- `src/router/index.ts` - 路由配置

**使用方式**:
```typescript
import { useRouter } from 'vue-router'

const router = useRouter()

// 跳转
router.push('/dashboard')

// 获取参数
const id = router.currentRoute.value.params.id
```

### 3. 状态管理

**关键文件**:
- `src/store/auth.ts` - 认证状态

**使用方式**:
```typescript
import { useAuthStore } from '@/store'

const authStore = useAuthStore()

// 登录
await authStore.login({ username, password })

// 获取用户信息
console.log(authStore.userInfo)

// 登出
authStore.logout()
```

### 4. 国际化

**关键文件**:
- `src/locales/*.json` - 语言文件

**使用方式**:
```vue
<template>
  <div>{{ $t('login.title') }}</div>
</template>

<script setup>
import { useI18n } from 'vue-i18n'

const { t } = useI18n()

console.log(t('login.title'))
</script>
```

### 5. 类型定义

**关键文件**:
- `src/typings/entities/*.d.ts` - 实体类型

**使用方式**:
```typescript
// 使用预定义类型
const user = ref<Entity.User | null>(null)

// 扩展类型
declare namespace Entity {
  interface NewEntity {
    id: number
    name: string
  }
}
```

---

## 🛠️ 常用工具

### 正则表达式 (src/constants/Regex.ts)

```typescript
import Regex from '@/constants/Regex'

// 验证邮箱
const isEmail = new RegExp(Regex.Email).test(email)

// 验证手机号
const isMobile = new RegExp(Regex.Mobile).test(mobile)
```

### 通用工具函数 (src/utils/index.ts)

```typescript
import { formatDate, debounce, throttle } from '@/utils'

// 格式化日期
const dateStr = formatDate(new Date(), 'YYYY-MM-DD')

// 防抖
const debouncedFn = debounce(fn, 300)
```

---

## 📝 开发规范

### 1. 组件命名

```vue
<!-- ✅ 推荐 -->
<UserProfile />
<UserList />

<!-- ❌ 不推荐 -->
<user-profile />
<userprofile />
```

### 2. API 命名

```typescript
// ✅ 推荐
fetchGetUserList
fetchCreateUser
fetchUpdateUser

// ❌ 不推荐
getUserList
getUser
requestUserList
```

### 3. 类型命名

```typescript
// ✅ 推荐
interface UserInfo { }
type UserRole = 'admin' | 'user'

// ❌ 不推荐
interface userInfo { }
type userrole = string
```

---

## 📖 详细文档索引

| 文档 | 路径 | 内容 |
|------|------|------|
| 前端请求 | `../doc/前端请求.md` | Alova封装、API调用 |
| JWT认证 | `../doc/JWT认证.md` | Token处理（参考） |
| API路由 | `../doc/API路由.md` | 后端路由（参考） |

---

## ⚠️ 开发注意事项

1. **使用别名**: 使用 `@/` 别名导入模块
2. **类型安全**: 为所有变量和函数添加类型
3. **错误处理**: 使用 try-catch 处理异步操作
4. **组件拆分**: 大组件拆分为小组件
5. **代码复用**: 使用 composables 复用逻辑

---

> 💡 **提示**: 开发新功能前请先阅读对应的详细文档。
