# Frontend 目录留档

> 📁 **文件位置**: `frontend/留档.md`  
> **最后更新**: 2026-07-20

---

## 最近更新（2026-07-19 ~ 2026-07-20）

- **Token 自动刷新**：`VITE_AUTO_REFRESH_TOKEN=Y`（推荐）；布局挂载恢复 `setupAutoRefresh`；登出 fire-and-forget + `force-logout`。
- **默认语言**：`VITE_DEFAULT_LANG=zhCN`；启动时若本地无 `lang`，用 `app-config.default_lang` 同步到 appStore。
- **登录失败提示**：`auth.login()` 对 `!isSuccess` / catch 给出明确 toast，不再静默。
- **Presence**：`composables/usePresence.ts` 默认心跳 **30s**（与后端配置对齐）；管理端改上报周期后 stop+start 热更新。
- **在线用户页**：同一用户多设备归并一行展示（设备 Tag + 单设备踢下线）；`adminApi` 在线接口类型为 `OnlineUserRow`。
- **仪表盘实名入口**：快捷/告警跳转改为 `users/realname`。
- **短信**：SmsTab 双轨说明 +「发送测试」；短信模板页强化双轨文案。
- **adminApi.finance**：懒加载财务模块；用户财务/提现页改走统一入口。
- **工作台**：API 失败 toast + 空状态；公告接真数据（点标题打开 MdPreview）。
- **站内公告（2026-07-20）**：
  - 管理端 `settings/announcements` + MdEditor；基本设置开关 `announcement_enabled`
  - 顶栏铃铛：通知 Tab=公告；消息/待办空态；点击可关 Modal 预览
  - Presence 收到 `announcement` 事件后刷新铃铛（含跨 Tab BroadcastChannel）
  - 已读按用户账号，不按浏览器
- **手机号**：`PhoneInput` + `dialCodes` / `utils/phone.ts`；对接公开 geo。
- **Iconify 离线**：`modules/iconify-offline.ts` 注册 `@iconify-json/icon-park-outline`。
- **设置页**：邮件/短信嵌套 Tab；邮件/短信模板从侧栏收敛到设置内；管理端 i18n 必补（见 `.cursor/rules/admin-i18n-required.mdc`）。
- **环境文件**：仍用 Vite 分层 `.env` + `.env.dev` + `.env.production`；**不要**去读仓库根后端 `.env`。

### 更早（2026-07-16）

- 管理端 API 运行时注入 `admin_api_path`；页面入口与 REST 前缀分离；删除前端 Demo。

---

## 环境变量约定

| 文件 | 用途 |
|------|------|
| `.env` | 公共默认（名称、路由、Token、语言等） |
| `.env.dev` | `pnpm dev`（`--mode dev`）覆盖：代理、本地 API |
| `.env.production` | 生产/embedded 同源等 |
| `.env.example` | 示例，可提交 |

与后端根 `.env` **分开维护**；仅需人工对齐 `VITE_ADMIN_BASE_PATH` ↔ `ADMIN_PATH`。

常用键：`VITE_DEFAULT_LANG=zhCN`、`VITE_API_URL`、`VITE_ADMIN_BASE_PATH`、`VITE_AUTO_REFRESH_TOKEN`。

---

## 📂 目录结构（精简）

```text
frontend/
├── src/
│   ├── components/common/     # PhoneInput、NovaIcon 等
│   ├── composables/           # usePresence
│   ├── modules/               # iconify-offline、i18n 等
│   ├── views/admin/           # 仪表盘/用户/在线/日志/设置/模板…
│   ├── views/user/            # 用户端
│   ├── router/                # admin.routes / routes.static / guard
│   ├── store/                 # auth / settings / app
│   └── service/api/           # admin|user|geo|session|app-config
├── locales/                   # zh_CN / en_US
├── .env / .env.dev / .env.production / .env.example
└── vite.config.ts
```

---

## 两套管理路径

| 配置 | 含义 | 默认 |
|------|------|------|
| `VITE_ADMIN_BASE_PATH` | 管理**页面**入口 | `/system-mgr` |
| 运行时 `admin_api_path` | 管理 **REST** 前缀 | `/admin` → `/api/v1/admin` |
| `VITE_ADMIN_API_PATH` | 仅回退 | `/admin` |

---

## 开发

```bash
cd frontend && pnpm install && pnpm dev
```

管理端新增文案必须同步 `locales/zh_CN.json` + `en_US.json`。

📖 [src/service/网络请求与API定义.md](src/service/网络请求与API定义.md) · [../doc/前端请求.md](../doc/前端请求.md) · [../doc/在线会话与Presence.md](../doc/在线会话与Presence.md)
