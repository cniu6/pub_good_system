# 逻辑封装与组合式函数 (Hooks)

## 简介
使用 Vue Composition API 封装的跨组件共享逻辑。

## 功能字段与函数
- `useBoolean`: 状态切换逻辑。
- `useEcharts`: 图表生命周期与自适应管理。
- `usePermission`: 权限控制判断逻辑。
- `useTableDrag`: 表格交互增强逻辑。

## 规范
- 必须以 `use` 开头命名。
- 复杂逻辑必须导出 TS 类型。
