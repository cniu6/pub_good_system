# 通用UI组件与业务封装 (Components)

## 简介
封装可复用的视图组件，提升前端开发效率。

## 功能字段与函数
- `AppLoading`: 系统级加载动画。
- `NaiveProvider`: UI 框架环境配置。
- `MarkDownEditor` / `RichTextEditor`: 编辑器封装。
- `Pagination`: 分页逻辑组件。

## 规范
- 组件必须支持 `props` 配置。
- 业务组件与 UI 组件应保持目录分离。
